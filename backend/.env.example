## Copia este archivo a `.env` y ajusta los valores

# Servidor HTTP del backend
PORT=4000

# Conexión MySQL (XAMPP suele usar 127.0.0.1:3306 y usuario root sin contraseña)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=inventario_db

# Clave para firmar JWT (cámbiala por algo largo y secreto)
JWT_SECRET=cambia_esta_clave_larga
